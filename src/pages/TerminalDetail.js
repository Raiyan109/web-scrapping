import React from 'react';

const TerminalDetail = ({ d }) => {
    return (
        <div>
            <h1>{d.name}</h1>
            <table>
                <thead>
                    <tr></tr>
                </thead>
                <tbody>

                </tbody>
            </table>
        </div>
    );
};

export default TerminalDetail;